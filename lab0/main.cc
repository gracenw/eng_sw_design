#include <iostream>

// I, Gracen Wallace, have successfully accessed my pace-ice account.
// I am able to transfer files from my pace-ice account to my local machine using an ftp client.  
// I can use either vim or nano directly on pace-ice. 
// I am ready for the next assignment on pace-ice. 

int main() {
        std::cout << "This (\") is a double quote.\n";
        std::cout << "This (\') is a single quote.\n";
        std::cout << "This (\\) is a backslash.\n";
        std::cout << "This (/) is a forward slash.\n";

        return 0;
}
